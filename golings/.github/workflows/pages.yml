name: Deploy docs to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'web/**'
      - 'info.toml'
      - 'exercises/**'
      - 'CURRICULUM.md'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # The generator embeds worked solutions (from the solution branch) into
      # the catalog's detail pages; the default checkout only has main.
      - name: Fetch solution branch
        run: git fetch --depth=1 origin solution:refs/remotes/origin/solution
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
      - name: Generate curriculum content
        run: go run ./web/gen
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install deps
        run: npm ci
        working-directory: web
      - name: Build site
        run: npm run build
        working-directory: web
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
